# Dev: leave VITE_API_URL unset — the Vite proxy forwards API + Socket.IO to
# the backend (override target with VITE_DEV_API_TARGET, port with VITE_PORT).
# Set an absolute URL only for production builds (Docker build arg).
# VITE_API_URL=http://localhost:8000
