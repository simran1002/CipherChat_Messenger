# Terraform working directory (providers, module clones) — never commit; the lock file is committed
infrastructure/terraform/.terraform/
# Local upload storage written by the backend
backend/data/
# Retired Node backend kept on disk only (holds a local .env); not part of the repository
chat-back/
